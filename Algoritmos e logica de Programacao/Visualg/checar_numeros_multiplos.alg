Algoritmo "Checar Multiplos"

Var

   x, y : inteiro

Inicio


   escreval("Digite dois numeros inteiros")
   leia(x)
   leia(y)
     se (x % y = 0) ou (y % x = 0) entao
       escreval(" Os numeros sao multiplo")
     senao
       escreval("Os numeros NAO sao multiplos")
   fimse

Fimalgoritmo